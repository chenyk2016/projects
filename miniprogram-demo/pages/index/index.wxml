<view class="index-page">
  <view class="item" bindtap="openWebview">
    <text>webview bridge页面</text>
  </view>

  <view class="item" wx:for="{{pages}}" data-path="{{item}}" wx:key="item" bindtap="openPath">
    {{index}}: {{item}}
  </view>
</view>